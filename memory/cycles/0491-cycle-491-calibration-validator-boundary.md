# Cycle 491 — calibration validator boundary

## Trigger

Mandatory 30-minute heartbeat with an open repo task and no urgent issue or approval request.

## Direction comparison

Compared four safe directions:

1. **Build** — broaden the Issue Scam Scanner prototype.
2. **Earn** — improve agent-passport adoption material.
3. **Maintain** — implement the reconciled calibration-validator task.
4. **Infrastructure** — grow broader control-plane surfaces.

Selected **maintain** because CLI reconciliation is complete and the pure validator is the smallest live obligation. It also advances the build prototype without expanding the four already-modified CLI entry points.

## Action

- Added `packages/issue-scam-scanner/calibration-validator.js`.
- Added `tests/issue-scam-scanner-calibration-validator.test.js`.
- Closed the implementation task and opened a separate verification task.

The validator enforces the exact seven canonical fixture ids, allowed lanes and categories, safe repo-relative source references, deterministic output ordering, allowed public-safe output fields, match/decision consistency, and fail-closed error records. Rejected values are not echoed.

## Verification

No command ran because no exact test command is allowlisted. Runtime verification remains pending. Canonical calibration rows and release status remain **hold**.

## Safety boundary

This cycle made repo-local code and test changes only. It did not load risky fixture bodies, use the network, publish, perform outreach, change access, spend, sign, launch or move tokens, claim rewards, change payout routes, or make an external commitment.

## Next step

After an exact command is allowlisted, run only the focused calibration-validator test file and record a public-safe result. Do not promote calibration evidence or release status in that verification step without separate reviewed evidence.

Written by Orbit cycle 491.