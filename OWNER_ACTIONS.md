# OWNER_ACTIONS.md — Open Items For The Repo Owner

> One-page distillation of `PLAN/SGATE_1.md`. The engineering side of Phase 1 is complete; the seven items below need the owner's hands (keys, GitHub settings, on-chain deploys) before S-GATE-1 can close and the token launch sequence (D-018) can begin.

Each item below lists the **what**, the **why**, the **copy-paste commands**, and the **acceptance check** (how to know it's done).

Track completion by ticking the box in `PLAN/SGATE_1.md`. Do not edit this file when an item closes — close it in `PLAN/SGATE_1.md` and refresh `PLAN/STATUS.md`'s CURRENT STATE block.

---

## 1. Enable GitHub Pages

**Why.** `public/dashboard.json` and `public/.well-known/orbit.json` are written every cycle and ready to serve from Pages. Until Pages is on, `orbit.horse` returns nothing.

**Do.**
```
# In the repo's Settings → Pages:
#   Source: GitHub Actions
#   Custom domain: orbit.horse
#   Enforce HTTPS: on
```
The workflow `.github/workflows/deploy-dashboard.yml` is already wired. `public/CNAME` is in the repo.

**Acceptance.** `curl -fsS https://orbit.horse/dashboard.json | jq .schema` returns `"orbit-dashboard/1"`.

---

## 2. Set `ORBIT_AGENT_SIGNER` repo variable

**Why.** Cycle proofs (D-006) sign with the wallet key at `ORBIT_WALLET_PRIVATE_KEY`. The agent refuses to sign unless `ORBIT_AGENT_SIGNER` matches that key's recovered address — defends against silent key swaps. Until the variable is set, signed-proof emission is off.

**Do.**
1. Generate (or pick) the agent EOA. Record its address.
2. Settings → Secrets and variables → Actions:
   - Variables: `ORBIT_AGENT_SIGNER = 0x...`
   - Secrets: `ORBIT_WALLET_PRIVATE_KEY = 0x...` (the matching key)
3. Recommended (per `PLAN/DEPLOY_PLAN.md`): hold the key in a workflow-scoped secret store; do not paste into local `.env`.

**Acceptance.** Next cycle's commit body includes `signer: 0x...` and `proof.signature` is non-null. `npx @orbit-house/verifier <receipt.json>` returns OK.

---

## 3. Provision Farcaster signer

**Why.** `src/agent/farcaster.js` posts daily casts (D-008) but defaults to `dryRun:true`. Goes live only when a signer is provisioned. Required for the Phase 1 criterion *"≥1 daily Farcaster cast for 14 days"*.

**Do.** Follow `PLAN/SPECS/FARCASTER_CAST_PIPELINE.md` §10:
1. Create a Farcaster account for Orbit.
2. Generate a signer at the Warpcast developer console.
3. Save credentials as repo secrets: `ORBIT_FARCASTER_FID`, `ORBIT_FARCASTER_SIGNER_PRIVATE_KEY`, `ORBIT_FARCASTER_SIGNER_PUBLIC_KEY`.
4. Set repo variable `ORBIT_FARCASTER_DRY_RUN = "false"`.

**Acceptance.** Cycle log shows `farcaster: cast posted (hash: 0x...)` and the cast is visible on Warpcast under Orbit's FID.

---

## 4. Deploy Treasury Safe on Base

**Why.** D-004 mandates Orbit's only custodial wallet is a Safe multisig on Base. No buyback, no live token deploy, no productive treasury work proceeds without the Safe address recorded in `memory/treasury.json`.

**Do.** Follow `PLAN/SPECS/TREASURY_SAFE_DEPLOY.md` — 8-item checklist. Highlights:
1. Deploy via the Safe app on Base mainnet. Owners: at least 2 EOAs you control. Threshold ≥ 2/3.
2. Send a tiny test transaction (e.g., 0.0001 ETH self-transfer) to confirm signing flow works.
3. Record the address in `memory/treasury.json.safe.address` and commit.
4. Set repo variable `ORBIT_TREASURY_SAFE = 0x...`.

**Acceptance.** `memory/treasury.json` has `safe.address` set + tx hash of the test transfer. `governance.json` reflects the Safe as the only signing path for external spend.

---

## 5. Publish SDK and scaffolder to npm

**Why.** `@orbit-house/sdk` and `create-orbit-repo` are publish-ready (8KB and 11-template tarballs respectively). Until they're on npm, adopters can only consume Orbit by cloning the repo — the S-016 onboarding push is impossible.

**Do.**
```
# Log in to npm with the orbit-house publisher account.
npm whoami   # verify

cd packages/orbit-sdk
npm publish --access public

cd ../create-orbit-repo
npm publish --access public

cd ../orbit-verifier
npm publish --access public

# Optional (Phase 2 tooling, can defer):
cd ../orbit-anchor && npm publish --access public
cd ../orbit-tool-example && npm publish --access public
```

**Acceptance.** Each package resolves at `https://www.npmjs.com/package/@orbit-house/<name>`. `npx create-orbit-repo demo-orbit` succeeds in a clean directory.

---

## 6. Configure AI provider

**Why.** Without `ORBIT_AI_PROVIDERS` + matching keys, every cycle falls through to the deterministic fallback path — Orbit moves, but it does not *think*. T-8 routing (commit `1abb992c`) needs at least one configured provider for auto-demote/promote to have a baseline.

**Do.** Settings → Secrets and variables → Actions:
- Variables: `ORBIT_AI_PROVIDERS` as a JSON array. Example:
  ```json
  [
    {"name":"anthropic","label":"Claude","apiBase":"https://api.anthropic.com/v1","model":"claude-opus-4-7","chatPath":"/messages","priority":1},
    {"name":"openai","label":"GPT","apiBase":"https://api.openai.com/v1","model":"gpt-4o-mini","chatPath":"/chat/completions","priority":2}
  ]
  ```
- Secrets: `ORBIT_AI_PROVIDER_KEYS` as JSON: `{"anthropic":"sk-ant-...","openai":"sk-..."}`.

**Acceptance.** Cycle log shows `aiRoute: configured (count: 2)` and `result.usage` is non-null in cycle commits. After ~3 cycles, `memory/state.json.aiRouting.providers` has entries with `successCount > 0`.

---

## 7. Verify 12-hour clean cycle stretch — **gates first operator payout**

**Why.** D-018 hard-blocks token launch until 24 consecutive cycles run without `REFUSAL` or `FAIL`. This is the headline `state.preLaunchVerified` flag — the master switch for every gated on-chain feature (buyback, clanker launch, mission-board staking, bounty market). It is also the final gate before any operator revenue can flow — both the 5% operator share and the 76% treasury share are zero until this closes and S-011/S-012 land $ORBIT on Base.

**Do.**
1. Once items 1–6 are done, let the GitHub Actions cron run for ~12 hours (default cron is hourly via `.github/workflows/orbit-cycle.yml`).
2. Watch `gh run list --workflow=orbit-cycle.yml --limit 30` for green runs only.
3. After 24 consecutive clean cycles, record the range in `memory/state.json`:
   ```json
   "firstCleanCycle": 57,
   "lastCleanCycle": 80,
   "preLaunchVerified": true
   ```
4. Commit and push.

**Acceptance.** `governance.json.gates.d018` reads `passed`. `assertTreasuryFloor` and other gated paths now consult `state.preLaunchVerified === true` and allow live execution. The buyback dry-run path can be flipped off per the runbook in `PLAN/SPECS/TREASURY_SAFE_DEPLOY.md`.

---

## After all seven close

Edit `PLAN/SGATE_1.md` → tick every box and append the gate closure block. Edit `PLAN/STATUS.md` → flip CURRENT PHASE to *Phase 2 — Token Launch Window* and set NEXT SESSION to **S-011** (Clanker v4 deploy dry run). Push.

That commit is the public artifact for *"S-GATE-1 closed, Orbit moves to Phase 2."*
